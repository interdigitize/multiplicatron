const product = function(num, num2){
  // if(isNaN(num) || isNaN(num2)){
  //   return NaN + '\n' + "Please enter a number.";
  // }
  return num * num2;
}

module.exports = product;



//var multiplyArr = [];

//if all form fields do not have a value, return NAN and append it to the DOM
//loop through each form field and add the value to an array
//loop through the array and reduce to the product
//return the product
//append the product to the DOM
